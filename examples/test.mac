Routine test.x [Type = mac]
procedure() methodimpl {
    w "hi"
    set x = 2
    if x {
        w "bye"
        q
    }
}

procedureTwo() 
    w "in goodbye \n"
    w "this is valid objectscript"

 if x {
    w "x exists"
 }

